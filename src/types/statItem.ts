/**
 * Props for the small label/value metric row used on market dashboards.
 */

/** Display props for a single labeled metric cell. */
export interface StatItemProps {
    label: string;
    value: string | number;
}
